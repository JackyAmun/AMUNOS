# AMUNOS CC 编译器使用手册

> 版本: v6.2
> 在 AMUNOS 上编译并运行 C 语言子集程序

---

## 目录

1. [概览](#一-概览)
2. [两种编译模式](#二-两种编译模式)
3. [原生模式支持的语法](#三-原生模式支持的语法)
4. [字节码模式支持的语法](#四-字节码模式支持的语法)
5. [示例程序](#五-示例程序)
6. [运行时调用约定](#六-运行时调用约定)
7. [变量与内存布局](#七-变量与内存布局)
8. [限制与注意事项](#八-限制与注意事项)

---

## 一、概览

CC 编译器为 AMUNOS 提供 C 语言编程能力。支持两种编译方式：

```
模式 A (字节码):  CC file.c   →  RUN       — 慢但功能多
模式 B (原生):    CC -x file.c →  LOAD .COM — 快，原生 x86 机器码
```

| 命令 | 作用 |
|------|------|
| `CC [-V] [-x] file.c` | 编译 C 源文件 |
| `-V` | 显示编译器版本 |
| `-x` | 生成原生 x86 机器码 (.COM) |
| `LOAD file.com` | 加载并执行原生程序 |
| `RUN` | 执行字节码程序 |

---

## 二、两种编译模式

### 模式 A — 字节码 (CC file.c + RUN)

基于 C4 编译器，编译为 39 条字节码指令，在虚拟机中解释执行。

**支持较完整**: 函数、指针、数组、if/while、递归、字符串。

### 模式 B — 原生 (CC -x file.c + LOAD file.com)

直接生成 x86 机器码，CPU 原生执行，速度快 10 倍以上。

**支持较简单**: 适合计算器、数值计算等简单程序。

> **建议**: 复杂程序用模式 A，简单交互/计算用模式 B。

---

## 三、原生模式支持的语法

### 1. 程序结构

```c
int main() {
    ...语句...
}
```

### 2. 输出 — printf

```c
printf(42);          // 输出数字 42
printf(1 + 2);       // 输出 3
printf(a + b);       // 输出变量 a+b
```

### 3. 输入 — input()

```c
int a;
a = input();         // 从键盘读一个数字到变量 a
```

输入方式：输入数字，用**空格**或**回车**结束。

### 4. 变量赋值

变量固定为 `a`, `b`, `c`, `d` 四个，对应内存槽 0x2000-0x200C：

```c
a = 10;
b = input();
c = a * 2;
```

### 5. 返回 — return

```c
return 0;            // 返回 0 (LOAD 显示 ret=0)
return a + b;        // 返回表达式结果
```

### 6. 表达式

支持 `+ - * / %` 和括号，优先级正确：

```c
printf(2 + 3 * 4);     // 14 (乘法优先)
printf((2 + 3) * 4);   // 20 (括号优先)
printf(10 / 3);        // 3 (整数除法)
printf(10 % 3);        // 1 (取余)
printf(a + b * c);     // 变量混合运算
```

---

## 四、字节码模式支持的语法

C4 子集（比原生模式更完整）：

### 数据类型
```c
int, char, 指针, 数组, enum, 字符串
```

### 语句
```c
if (条件) { ... } else { ... }
while (条件) { ... }
return 表达式;
```

### 运算符
```c
+ - * / % < > <= >= == != = & * [ ] 
&& || ! ?: 前缀 ++ -- (部分)
```

### 内置函数
```c
printf(...);    // 输出
```

### 示例 — 斐波那契
```c
int fib(int n) {
    if (n <= 2) return 1;
    return fib(n-1) + fib(n-2);
}
int main() {
    printf(fib(10));   // 输出 55
    return 0;
}
```

---

## 五、示例程序

### 示例 1: 计算器 (原生模式)

**文件: CALC.C**
```c
int main() {
    a = input();      // 读第一个数
    b = input();      // 读第二个数
    printf(a + b);    // 输出和
    printf(a * b);    // 输出积
    return a - b;     // 返回差
}
```

**运行:**
```
A:\> B:
B:\> CC -x CALC.C
B:\> LOAD CALC.COM
> 12                 ← 输入 a
> 5                  ← 输入 b
17 60                ← a+b, a*b 输出
ret=7                ← a-b 返回值
```

### 示例 2: 常量表达式 (原生)

```c
int main() {
    printf(1 + 2);      // 3
    printf(3 * 4);      // 12
    printf(100 - 1);    // 99
    return 0;
}
```

### 示例 3: 基本输出 (原生)

**文件: HW.C**
```c
int main() {
    printf(42);       // 42
    printf(123);      // 123
    return 0;
}
```

### 示例 4: 斐波那契 (字节码)

```c
int fib(int n) {
    if (n <= 2) return 1;
    return fib(n-1) + fib(n-2);
}
int main() {
    printf(fib(10));
    return 0;
}
```

**运行:**
```
B:\> CC FIB.C
B:\> RUN
```

---

## 六、运行时调用约定

原生 .COM 程序通过**固定地址**与内核交互：

| 地址 | 功能 |
|------|------|
| `0x1000` | `CALL` 此地址 = 输出数字 (读 `[0x2000]`) |
| `0x1010` | `CALL` 此地址 = 读键盘输入 (返回 EAX) |

变量内存槽：

| 变量 | 地址 |
|------|------|
| `a` | 0x2000 |
| `b` | 0x2004 |
| `c` | 0x2008 |
| `d` | 0x200C |

---

## 七、变量与内存布局

- .COM 程序加载到 `0x100000` 执行
- 参数通过固定内存槽传递（避免复杂调用约定）
- 函数返回值在 EAX 寄存器

```
0x2000 ┌─────┐  ← 变量 a
0x2004 ├─────┤  ← 变量 b
0x2008 ├─────┤  ← 变量 c
0x200C ├─────┤  ← 变量 d
       └─────┘
```

---

## 八、限制与注意事项

### 原生模式 (-x)

- ❌ 不支持 `for`、`switch`、`struct`、`float`、字符串
- ❌ 不支持函数定义（只有 `main`）
- ❌ 不支持 `if`/`while` 语句（表达式可用）
- ✅ 变量固定为 a-d 四个
- ✅ 支持 `input()` 输入和 `printf()` 输出

### 字节码模式

- ❌ 不支持 `struct`、`float`、`for`、`switch`
- ❌ 不支持预处理 (`#include`, `#define`)
- ✅ 支持函数、递归、指针、数组

### 编译错误处理

- 编译失败会显示 `Compile error.`
- 加载无效 .COM 会报 `Invalid .COM format`
- 程序崩溃会触发故障处理，显示 `*** FAULT ***` 而不是重启

---

## 附：B 盘自带的测试文件

| 文件 | 内容 |
|------|------|
| `HW.C` | 基本 printf 输出 |
| `RETVAL.C` | 返回常量 |
| `COUNT5.C` | 连续输出 1-5 |
| `CALC.C` | 计算器 (输入+运算) |
| `LOOP99.C` | 循环输出 99 |

---

*AMUNOS CC — 让系统自己编写和运行程序*
